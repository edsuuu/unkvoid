{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "App permissions: automatic updates and restart after installation.",
  "windows": [
    "main"
  ],
  "permissions": [
    "core:default",
    "updater:default",
    "process:allow-restart",
    "deep-link:default",
    "opener:allow-open-url"
  ]
}
