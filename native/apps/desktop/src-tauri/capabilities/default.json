{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "App permissions: automatic updates, restart after installation, and window fullscreen.",
  "windows": [
    "main"
  ],
  "permissions": [
    "core:default",
    "core:window:allow-set-fullscreen",
    "updater:default",
    "process:allow-restart"
  ]
}
